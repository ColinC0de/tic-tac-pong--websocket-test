export const PI = 3.14;
export const string = 'string';
export function add (one, two) {
  return one + two;
}

export class Console {
  
}

export default {
  add (one, two) {
    return one + two;
  }
};
